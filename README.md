# flight-booking
A flight booking agent
# FlightFinder Pro with Pydantic AI

A flight booking agent built with Pydantic AI, Browserbase, and Kayak integration.

## Setup

1. Install dependencies:
```bash
pip install -r requirements.txt
playwright install


Run the app using 
```bash 
    streamlit run yourscript.py
```